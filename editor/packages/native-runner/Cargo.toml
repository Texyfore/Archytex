[package]
name = "native-runner"
version = "0.1.0"
edition = "2021"
description = "A native runner for the Archytex editor"
authors = ["Gulyás Arnold"]

[dependencies]
winit = "0"
app = { path = "../app" }