[package]
name = "tools"
version = "0.1.0"
edition = "2018"

[dependencies]
web-sys = { version = "0.3.55", features = ["Window", "Document", "HtmlCanvasElement"] }
winit = { version = "0.25.0", features = ["web-sys"] }